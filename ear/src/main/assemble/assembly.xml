<project name="web-mobile-dm-ear-assembly"
         default="build"
         xmlns:nx="urn:nuxeo-build"
         xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml" uri="urn:nuxeo-artifact" />

  <target name="init" unless="init.done">
    <property name="outdir" value="${maven.project.build.directory}" />
    <property name="nuxeo.ear" value="${outdir}/nuxeo.ear" />

    <antcall target="expand" />
    <property name="init.done" value="true" />
  </target>

  <target name="expand">
    <artifact:nuxeo-expand includeTestScope="true" groupPrefixes="org.nuxeo" />
    <!-- For Nuxeo IDE being able to use that distribution as a SDK -->
    <artifact:print output="${outdir}/artifacts-web-mobile-dm.properties" mode="sdk" />
    <artifact:print output="${outdir}/test-artifacts-web-mobile-dm.properties" mode="sdk" scopes="test" />
    <!-- Prints the graph for debugging purpose -->
    <artifact:print output="${outdir}/dependency-tree.log" />
  </target>

  <target name="build" depends="init" description="Build EAR">
    <echo level="info">Building EAR</echo>
    <delete failonerror="false" dir="${nuxeo.ear}" />
    <mkdir dir="${nuxeo.ear}" />
    <copy todir="${nuxeo.ear}/bundles" overwrite="true">
      <artifact:set>
        <includes>
          <artifact groupId="org.nuxeo*" />
        </includes>
        <excludes>
          <artifact groupId="org.nuxeo.lib*" />
          <artifact scope="test" />
          <artifact scope="provided" />
          <artifact type="pom" />
        </excludes>
      </artifact:set>
    </copy>

    <copy todir="${nuxeo.ear}/lib" overwrite="true">
      <artifact:set>
        <includes>
          <artifact groupId="!org.nuxeo*" scope="!test" />
          <artifact groupId="org.nuxeo.lib*" scope="!test" />
        </includes>
      </artifact:set>
    </copy>
    <nx:rmdups dir="${nuxeo.ear}/lib" />

    <copy file="${outdir}/artifacts-web-mobile-dm.properties" todir="${nuxeo.ear}" />
    <copy file="${outdir}/test-artifacts-web-mobile-dm.properties" todir="${nuxeo.ear}" />

    <zip destfile="${outdir}/${maven.project.artifactId}-${marketplace.package.version}.zip" basedir="${nuxeo.ear}" />
    <artifact:attach file="${outdir}/${maven.project.artifactId}-${marketplace.package.version}.zip" type="zip" />
  </target>

</project>
